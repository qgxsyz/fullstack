<h2><%= @course.course_name %> 新增章节</h2>
<%= simple_form_for [:admin, @course, @chapter] do |f| %>

    <%= f.input :chapter_name, label: "章节名称" %>

  <%= f.submit "提交", data: { disable_with: "提交中..." } %>

<% end %>
