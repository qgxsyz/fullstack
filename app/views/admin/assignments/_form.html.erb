<%= simple_form_for [:admin, @section, @assignment] do |f| %>

    <%= f.input :assignment_name, label: "作业名字" %>
    <%= f.input :description, label: "作业描述"%>

    <%= f.submit "提交", data: { disable_with: "提交中..." }, class: "btn btn-primary" %>

<% end %>
